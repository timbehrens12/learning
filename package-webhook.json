{
  "name": "studylayer-webhooks",
  "version": "1.0.0",
  "description": "Stripe webhook server for StudyLayer",
  "main": "stripe-webhook-server.js",
  "scripts": {
    "start": "node stripe-webhook-server.js",
    "dev": "nodemon stripe-webhook-server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "stripe": "^15.4.0",
    "@supabase/supabase-js": "^2.43.1",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
